# try proxistat via getnearbyblocks

# library(EJAM)
# tp = testpoints_100_dt[1:3,]
# 
# x <- EJAM::getblocksnearby(testpoints(3), cutoff = 3.1, avoidorphans = TRUE, quadtree = localtree)
# 
# dc = blockpoints_area_pop[substr(blockpoints_area_pop$blockfips,1,2) == '11', ]
# leaflet::leaflet(dc[,.(lon,lat)])  |> leaflet::addTiles() |> leaflet::addCircles(lng = ~lon, lat= ~lat, radius = 10 )  
