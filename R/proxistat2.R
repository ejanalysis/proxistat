proxistat2 <- function(fromrow, torow, d, area) {
  
  # Function to simply aggregate over all torows
  # torow and d must be same length vectors
  # area and radius must be same length vectors if specified
  
  
}
